import { makeStyles } from '@material-ui/core/styles';
export default makeStyles(theme => ({
  streams: {
    display: 'flex',
    justifyContent: 'center'
  },
  container: {
    width: '100vw',
    height: '100vh',
    display: 'flex',
    flexDirection: 'row'
  }
}));
